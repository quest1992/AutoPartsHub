'use client';
import { FormEvent, Suspense, useEffect, useState } from 'react'; import { useRouter, useSearchParams } from 'next/navigation'; import { ProtectedLayout } from '../../../components/protected-layout'; import { createSale, inventoryOne, InventoryItem } from '../../../lib/api';
function Form(){const id=useSearchParams().get('inventoryItem');const router=useRouter();const[item,setItem]=useState<InventoryItem|null>(null);const[q,setQ]=useState('1');const[error,setError]=useState('');useEffect(()=>{if(id)inventoryOne(id).then(setItem).catch(e=>setError(e.message))},[id]);async function submit(e:FormEvent){e.preventDefault();if(!item)return setError('Выберите товар из карточки склада');try{const sale=await createSale({shopId:item.shop.id,items:[{inventoryItemId:item.id,quantity:Number(q)}]});router.replace(`/sales/${sale.id}`)}catch(e){setError(e instanceof Error?e.message:'Недостаточно товара или ошибка продажи')}}return <ProtectedLayout><h1>Создать продажу</h1><form onSubmit={submit}><p>{item?`${item.partCatalogItem.name} · ${item.price} ${item.currency}`:'Выберите товар'}</p><input value={q} onChange={e=>setQ(e.target.value)} type="number" min="1"/>{error&&<p>{error}</p>}<button>Оформить продажу</button></form></ProtectedLayout>};export default function Page(){return <Suspense><Form/></Suspense>}
