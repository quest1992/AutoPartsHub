'use client';
import { useRouter } from 'next/navigation'; import { InventoryForm, InventoryValues } from '../../../components/inventory-form'; import { ProtectedLayout } from '../../../components/protected-layout'; import { createInventory } from '../../../lib/api';
export default function NewInventoryPage(){const router=useRouter();async function save(v:InventoryValues){await createInventory({partCatalogItemId:v.partCatalogItemId,brand:v.brand||undefined,sku:v.sku||undefined,oemNumber:v.oemNumber||undefined,condition:v.condition,price:Number(v.price),currency:v.currency,quantity:Number(v.quantity),minQuantity:Number(v.minQuantity),location:v.location||undefined,notes:v.notes||undefined,isActive:v.isActive});alert('Товар успешно создан');router.replace('/inventory')}return <ProtectedLayout><h1 className="mb-6 text-2xl font-bold">Добавить товар</h1><InventoryForm onSave={save}/></ProtectedLayout>}
